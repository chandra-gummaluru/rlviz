// Input data for Log Optimal Policy use case - just the user-given name from the "Name this
// policy" modal (namePolicyModal.js).
class LogOptimalPolicyInputData {
    constructor(name) {
        this.name = name;
    }
}

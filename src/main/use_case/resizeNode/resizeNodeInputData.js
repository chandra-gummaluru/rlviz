// Input data for resizing a node
class ResizeNodeInputData {
    constructor(nodeId, oldSize, newSize) {
        this.nodeId = nodeId;
        this.oldSize = oldSize;
        this.newSize = newSize;
    }
}
